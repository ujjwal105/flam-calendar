import { Button } from "./components/ui/button";

function App() {
  return (
    <div className="flex items-center justify-center h-screen bg-black">
      <Button className="bg-[#d2fc04] font-bold text-lg cursor-pointer hover:bg-[#b3d200]">
        Flam Calendar
      </Button>
    </div>
  );
}

export default App;
